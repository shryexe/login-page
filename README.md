# Login Page

A React-based login page project.

## Projects

This workspace contains two projects:

### my-app
A Create React App project.

```bash
cd my-app
npm install
npm start
```

### Hi-Anime
A Vite + React + TypeScript project.

```bash
cd Hi-Anime/Hi-Anime
npm install
npm run dev
```

## Getting Started

1. Clone the repository
2. Navigate to the desired project folder
3. Install dependencies with `npm install`
4. Run the development server using the commands above
